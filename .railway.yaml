# Railway部署配置文件
services:
  - type: web
    name: air-agent
    runtime: python
    plan: free
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
